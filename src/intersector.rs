pub struct Intersector {
}

impl Intersector {
    pub fn new() -> Self {
        Intersector {
        }
    }

    pub fn intersect(&self, required: Vec<u64>, forbidden: Vec<u64>) -> Vec<u16> {
        todo!()
    }
}